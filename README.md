This is the react-firebase template that I created.

It includes (or will) the following libraries and tools

React
Firebase
Ant Design 
Styled Components


* `npm install yarn`
* `yarn`
* `yarn start`
* visit http://localhost:3000/
