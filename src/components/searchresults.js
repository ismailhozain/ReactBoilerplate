import React from 'react';
import {Input, Icon} from 'antd';

const SearchResults = () => (
    <div>
        <Input>
            <Icon>

            </Icon>
        </Input>
    </div>
);
export default (SearchResults);
